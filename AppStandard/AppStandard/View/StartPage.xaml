<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:i18n="clr-namespace:AppStandard;assembly=AppStandard"
             xmlns:forms="clr-namespace:Plugin.MediaManager.Forms;assembly=Plugin.MediaManager.Forms"  
             x:Class="AppStandard.StartPage" Title="Home">

 <ScrollView>
    <StackLayout>
        <Label Margin="20" Text="{i18n:Translate CaptionLabel}"></Label>
        <Button Text="Pick Foto" Command="{Binding PickFotoCommand}" ></Button>
        <Button Text="Take Foto" Command="{Binding TakeFotoCommand}" ></Button>
        <Button Text="Pick Video" Command="{Binding PickVideoCommand}" ></Button>
        <Button Text="Take Video" Command="{Binding TakeVideoCommand}" ></Button>
        <Image Source="{Binding SelectedImage.Source}" WidthRequest="100" HeightRequest="200" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Margin="5"></Image>
        <forms:VideoView HeightRequest="202" WidthRequest="202" />  
        <Button Text="Play Video" Command="{Binding PlayVideoCommand}" ></Button>
        <Button Text="Stop Video" Command="{Binding StopVideoCommand}" ></Button>
    </StackLayout>     
  </ScrollView>
</ContentPage>
