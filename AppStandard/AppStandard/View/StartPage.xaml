<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:i18n="clr-namespace:AppStandard;assembly=AppStandard"
             xmlns:forms="clr-namespace:Plugin.MediaManager.Forms;assembly=Plugin.MediaManager.Forms"  
             x:Class="AppStandard.StartPage" Title="Home" BackgroundColor="Gray">

 <ScrollView>
    <StackLayout Margin="100">
        <Label Text="{i18n:Translate FirstNameLabel}"></Label>
        <StackLayout Padding="5" BackgroundColor="Teal">
            <Label Text="{Binding FirstName}" TextColor="White"></Label>
        </StackLayout>
             
        <Button Margin="0,40,0,0" WidthRequest="150" HorizontalOptions="CenterAndExpand" Text="{i18n:Translate EditNameText}" BackgroundColor="Green" TextColor="White" FontSize="14" Command="{Binding EditCommand}"></Button>  
    </StackLayout>     
  </ScrollView>
</ContentPage>
