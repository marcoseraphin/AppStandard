<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:i18n="clr-namespace:AppStandard;assembly=AppStandard"
             x:Class="AppStandard.EditPage" BackgroundColor="Gray">
    
  <ScrollView>
    <StackLayout Margin="100">
        <Label Text="{i18n:Translate FirstNameLabel}"></Label>
        <StackLayout Padding="5" BackgroundColor="Teal">
            <Entry Text="{Binding EditFirstName}" TextColor="Red"></Entry>
        </StackLayout>
             
        <Button Margin="0,40,0,0" WidthRequest="150" HorizontalOptions="CenterAndExpand" Text="{i18n:Translate SaveNameText}" BackgroundColor="Green" TextColor="White" FontSize="14" Command="{Binding SaveCommand}"></Button>  
    </StackLayout>     
  </ScrollView>    
  
</ContentPage>
